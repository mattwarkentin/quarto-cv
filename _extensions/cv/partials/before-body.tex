% Author name
\centerline{
  \huge \bf $author$ $if(orcid)$\orcidlink{$orcid$}$endif$ $if(scholar)$\MYhref{https://scholar.google.com/citations?user=$scholar$&hl=en}{\aiGoogleScholar}$endif$
}


\vspace{2 mm}

\hrule

\vspace{2 mm}

% Job title
$if(title)$\moveleft.5\hoffset\centerline{$title$}$endif$

% Affiliations
$if(affiliation)$
$for(affiliation)$
\moveleft.5\hoffset\centerline{$affiliation$}
$endfor$
$endif$

% Address
$if(address)$\moveleft.5\hoffset\centerline{$address$}$endif$

% Socials
\moveleft.5\hoffset\centerline{
  $if(email)$$if(fontawesome)$\faEnvelopeO \hspace{0.1mm}$else$E-mail:$endif$ \href{mailto:$email$}{\tt $email$} \hspace{1 mm}$endif$

  $if(phone)$$if(fontawesome)$ \faMobilePhone \hspace{0.1mm}$else$Phone:$endif$  $phone$  \hspace{1 mm} $endif$

  $if(github)$$if(fontawesome)$\faGithub \hspace{0.1mm}$else$Github:$endif$ \href{http://github.com/$github$}{\tt $github$} \hspace{1mm} $endif$

  $if(twitter)$$if(fontawesome)$\faTwitter \hspace{0.1mm}$else$Twitter:$endif$ \href{https:/twitter.com/$twitter$}{\tt $twitter$} \hspace{1mm} $endif$

  $if(web)$$if(fontawesome)$\faGlobe \hspace{0.1mm}$else$Web:$endif$ \href{http://$web$}{\tt $web$}  $endif$

  $if(updated)$ | Updated: \today$endif$
}

\vspace{2 mm}

\hrule

% Section rules for level-1 headers
$if(section-rules)$
\newcommand{\tmpsection}[1]{}
\let\tmpsection=\section
\renewcommand{\section}[1]{\tmpsection{#1}\hrule\nobreak}
$endif$

% Headers and footers
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[R]{\thepage\ of \pageref*{LastPage}}
\fancyhead[L]{\ifthenelse{\value{page}=1}{}{$author$}}
$if(logo)$\fancyhead[R]{\ifthenelse{\value{page}=1}{\includegraphics[width=25mm]{$logo$}}{}}$endif$
